// Public Web OAuth client ID (Google Cloud client_type 3).
// This is NOT a secret — the same value appears in google-services.json.
// CI overwrites this file from GOOGLE_SERVICES_JSON_BASE64 before the Android build.
// Leave empty for local web-only work; Android external-browser Google Sign-In needs it.
export const GOOGLE_WEB_CLIENT_ID = "";
